class piller{
    constructor(x,y,w,h){
    this.x=x;
    this.y=y;
    this.width=w;
    this.height=h;
    }
    display(){
        if(frameCount%80===0){
    var a=createSprite(this.x,this.y,this.width,this.height)
    a.velocityX=-4;
    score+=0.5
    
    if(b1.a.isTouching(a)||b1.a.isTouching(a)){
        gameState=0;
    
    }
    
    }
    
}
    
    }